// Proxy disabled - using direct API_URL in fetch calls
// const { createProxyMiddleware } = require('http-proxy-middleware');

module.exports = function(app) {
  // No proxy needed - frontend uses API_URL directly
};
